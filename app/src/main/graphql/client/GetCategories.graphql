query GetCategories($first: Int!) {
    collections(first: $first) {
        nodes {
            id
            title
            handle
            image {
                url(transform: { maxWidth: 200, maxHeight: 200 })
                altText
            }
        }
    }
}
